### Endpoints
- GET /search/{alias} (get the shortened url)
- GET /all (get all shortened urls created from visitors)
- DELETE /delete/{alias} (delete a specific shortened url)
- POST /upload

### SQLite Helpers
- READ insertURL
- READ showAll
- DELETE deleteURL
- CREATE insertURL